const config = {
    client: 'mysql',
    connection: {
        host:'127.0.0.1',
        user: 'root',
        password: 'nicokatz',
        database: 'desafio_sql'
    }
}

export default { config }